/**
 * Sdílená šířka hlavního obsahu stránek.
 *
 * Na mobilu úzký čitelný sloupec (`max-w-3xl` ≈ 768px), na PC (`lg:` a výš)
 * širší plocha (`max-w-6xl` ≈ 1150px). Díky tomu se na velké obrazovce míň
 * scrolluje a obsah může jít do více sloupců. Používej na kontejneru společně
 * s `mx-auto w-full`.
 */
export const PAGE_WIDTH = "max-w-3xl lg:max-w-6xl";
